services:
  - type: web
    name: discord-command-monitor-bot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DISCORD_TOKEN
        sync: false
    healthCheckPath: /
